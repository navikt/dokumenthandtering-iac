apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: privat-dok-journalpost-endret-q1
  namespace: q1
  labels:
    team: teamdokumenthandtering
spec:
  pool: {{pool}}
  config:
  {{#each config as |value key|}}
     {{key}}: {{value}}
  {{/each}}
  acl:
    - team: teamdokumenthandtering
      application: joarkhendelser
      access: read
    - team: teamdokumenthandtering
      application: teamdokumenthandtering-goldengate
      access: write